{
  "nio/Debounce": {
    "version": "0.1.0",
    "description": "After a signal flows through the block, filter out following signals for [interval] seconds.",
    "categories": [
      "Signal Flow"
    ],
    "properties": {
      "group_by": {
        "title": "Group By",
        "type": "Type",
        "description": "The value by which signals are grouped.",
        "default": null
      },
      "interval": {
        "title": "Debounce Interval",
        "type": "TimeDeltaType",
        "description": "Amount of time to wait before allowing another signal in a matching group.",
        "default": {
          "microseconds": 0,
          "days": 0,
          "seconds": 1
        }
      }
    },
    "inputs": {
      "default": {
        "description": "Any list of signals."
      }
    },
    "outputs": {
      "default": {
        "description": "The first signal for each group, every interval."
      }
    },
    "commands": {
      "groups": {
        "description": "Display the existing groups.",
        "params": {}
      }
    }
  }
}