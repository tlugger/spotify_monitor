{
    "exclude": true,
    "fields": [
        {
            "formula": "{{ $device if hasattr($, 'device') else {'name': \"nothing\", 'is_active': True} }}",
            "title": "device"
        },
        {
            "formula": "{{ $token }}",
            "title": "token"
        },
        {
            "formula": "[[AUTHORIZED_DEVICES]]",
            "title": "authorized"
        }
    ],
    "log_level": "NOTSET",
    "name": "GrabDevices",
    "type": "Modifier",
    "version": "1.0.0"
}